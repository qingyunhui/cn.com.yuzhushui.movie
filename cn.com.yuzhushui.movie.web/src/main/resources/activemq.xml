<!-- <?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:amq="http://activemq.apache.org/schema/core"
	xmlns:jms="http://www.springframework.org/schema/jms"
	xsi:schemaLocation="http://www.springframework.org/schema/beans   
        http://www.springframework.org/schema/beans/spring-beans-4.0.xsd   
        http://www.springframework.org/schema/context   
        http://www.springframework.org/schema/context/spring-context-4.0.xsd
        http://www.springframework.org/schema/jms
        http://www.springframework.org/schema/jms/spring-jms-4.0.xsd
        http://activemq.apache.org/schema/core
        http://activemq.apache.org/schema/core/activemq-core-5.8.0.xsd">

	配置activemq的连接url，及登陆用户、密码
	<amq:connectionFactory id="amqConnectionFactory" 
        brokerURL="${default_broker_url}" 
        userName="${default_user}" 
        password="${default_password}" />
        
	配置JMS连接工厂
    <bean id="connectionFactory"
        class="org.springframework.jms.connection.CachingConnectionFactory">
        <constructor-arg ref="amqConnectionFactory" />
        <property name="sessionCacheSize" value="100" />
    </bean>
	
	
	Queue begin
	
	定义消息队列（Queue）
    <bean id="queueDestination" class="org.apache.activemq.command.ActiveMQQueue">
        定义消息队列名称
        <constructor-arg>
            <value>${default_queue}</value>
        </constructor-arg>
    </bean>
    
    配置JMS模板（Queue），Spring提供的JMS工具类，它发送、接收消息。
    <bean id="jmsQueueTemplate" class="org.springframework.jms.core.JmsTemplate">
        <property name="connectionFactory" ref="connectionFactory" />
        <property name="defaultDestination" ref="queueDestination" />
        <property name="receiveTimeout" value="${default_timeout}" />
        true是topic，false是queue，默认是false，此处显示写出false
        <property name="pubSubDomain" value="false" />
    </bean>
	 
	配置消息队列监听者
	<bean id="defaultQueueMessageListener" class="qing.yun.hui.activemq.listener.DefaultQueueMessageListener"/>  
	
	显示注入消息监听容器（Queue），配置连接工厂，监听的目标是queueDestination，监听器是上面定义的监听器
	<bean id="defaultMessageListenerByQueue" class="org.springframework.jms.listener.DefaultMessageListenerContainer">
        <property name="connectionFactory" ref="connectionFactory" />
        <property name="destination" ref="queueDestination"  />
        <property name="messageListener" ref="defaultQueueMessageListener" />
    </bean>
    
    Queue end
    
    Topic begin
    
    <bean id="topicDestination" class="org.apache.activemq.command.ActiveMQTopic">
        <constructor-arg>
            <value>${default_topic}</value>
        </constructor-arg>
    </bean>
    <bean id="jmsTopicTemplate" class="org.springframework.jms.core.JmsTemplate">
        <property name="connectionFactory" ref="connectionFactory" />
        <property name="defaultDestination" ref="topicDestination" />
        <property name="receiveTimeout" value="${default_timeout}" />
        <property name="pubSubDomain" value="true" />
    </bean>
	
	<bean id="defaultTopicMessageListener" class="qing.yun.hui.activemq.listener.DefaultTopicMessageListener"/>
    
	<bean id="defaultMessageListenerByTopic" class="org.springframework.jms.listener.DefaultMessageListenerContainer">
        <property name="connectionFactory" ref="connectionFactory" />
        <property name="destination" ref="topicDestination"  />
        <property name="messageListener" ref="defaultTopicMessageListener" />
    </bean>
	
	Topic end
	
</beans>   -->